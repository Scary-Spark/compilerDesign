function removeWhiteSpace(str) {
  let count = 0;
  let i = 0;

  while (str[i] !== undefined) {
    // keep going until no character

    count++;
    i++;
  }

  return i;
}

let str = "   int   a = 5;   ";
console.log(`Length of the string: ${lengthCount(str)}`);
